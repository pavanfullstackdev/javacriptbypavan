// we can declare the variables using var,let,const

const  petName = 'orangecat';
let petId = '23';
var petColor = 'black'
eyeColor ='red';

console.table([petName,petId,petColor,eyeColor])

// We never use var to assign a variable to avoide block & functional scope errors

//we can reassign the variable with var key

//we can assign any variable without keyword but its bad practice
